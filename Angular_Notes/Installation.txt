npm install -g @angular/cli  -=> command to install it locally
ng bew "project name" 
here is "ng" is the command used to interact with the Angular Command Line Interface (CLI)

to start the project command is => npm start